// initial state
const state = () => ({
  urls: []
})

// getters
const getters = {}

// actions
const actions = {
  getUrls({ commit }) {

  }
}

// mutations
const mutations = {
  setUrls(state, urls) {
    
  }
}